# --- Core Audio Streaming & Processing ---
# Captures live microphone input in real-time memory buffers
sounddevice>=0.4.6

# Numerical operations and array manipulation for PCM audio chunks
numpy>=1.24.0

# --- Voice Activity Detection (VAD) Engine ---
# High-performance ONNX runtime required to execute Silero VAD locally
onnxruntime>=1.16.0

# --- Audio File I/O ---
# Lightweight WAV file writer to flush speech buffers for Whisper.cpp inference
wavio>=0.0.8

# --- Optional / Recommended for OS Automation & Testing ---
# Uncomment below if your team decides to build system keyboard/mouse automation in Python:
# pyautogui>=0.9.54
# pywinauto>=0.6.8  # (Windows-specific window control)